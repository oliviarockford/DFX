    struct Grid { double cspan,cstep,dspan,dstep; int tspan,tstep; };
    std::array<Grid,2> grids = {{{2.0,0.5,0.20,0.05,std::max(1,sps/24),std::max(1,sps/96)},{0.5,0.125,0.06,0.01,std::max(1,sps/96),1}}};
    for(auto g:grids){double localBest=-1e99;double c0=bestC,d0=bestD;int t0=bestT;for(double c=c0-g.cspan;c<=c0+g.cspan+1e-12;c+=g.cstep){for(double d=d0-g.dspan;d<=d0+g.dspan+1e-12;d+=g.dstep){int tmin=std::max(0,t0-g.tspan);int frameSamples=(frameIndex+layout(p).total_symbols)*sps;int maxFitTiming=std::max(0,int(x.size())-frameSamples);int tmax=std::min({sps-1,t0+g.tspan,maxFitTiming});if(tmin>tmax)tmin=tmax;for(int t=tmin;t<=tmax;t+=g.tstep){double m=known_symbol_ml_score(x,p,t,frameIndex,c,d);if(m>localBest){localBest=m;bestC=c;bestD=d;bestT=t;}}}}best=localBest;}timing=bestT;center=bestC;drift=bestD;}

double sync_energy_score(const std::vector<double>&x,const Profile&p,int timing,int frameIndex,double center){int sps=int(FS/p.baud);double score=0.0;for(size_t k=0;k<SYNC.size();k++){int pos=timing+(frameIndex+int(k))*sps;if(pos+sps>(int)x.size())return -1e99;double f=BASE_FREQ+SYNC[k]*p.spacing+center;double e=goertzel(x,pos,sps,f);double off=0.5*(goertzel(x,pos,sps,f-p.spacing)+goertzel(x,pos,sps,f+p.spacing))+1e-12;score+=std::log((e+1e-12)/off);}return score/double(SYNC.size());}

double soft_pattern_confidence(const std::vector<std::array<double,8>>&pw,int start,const std::vector<int>&pat){if(start<0||start+(int)pat.size()>(int)pw.size())return -1e99;double score=0.0;for(size_t i=0;i<pat.size();i++){const auto&a=pw[start+int(i)];int expected=pat[i];double e=a[expected]+1e-30;double bestOther=1e-30;for(int s=0;s<8;s++)if(s!=expected)bestOther=std::max(bestOther,a[s]);score+=std::log(e/bestOther);}return score/double(pat.size());}

DecodeResult local(const std::vector<double>&x,const Profile&p,double hint){int sps=int(FS/p.baud);auto L=layout(p);int bestT=0,bestIdx=0;double bestC=hint,bestM=-1e99;std::vector<int>timings;int tstep=(p.name=="DEEP"?std::max(1,sps/16):std::max(1,sps/8));int maxInitialTiming=std::max(0,int(x.size())-(2+layout(p).total_symbols)*sps);if(maxInitialTiming==0)timings.push_back(0);else for(int t=0;t<sps&&t<=maxInitialTiming;t+=tstep)timings.push_back(t);double coarseStep=(p.name=="DEEP"?2.0:5.0);for(double c=hint-50;c<=hint+50+1e-9;c+=coarseStep){for(int idx=0;idx<=2;idx++){for(int t:timings){double m=sync_energy_score(x,p,t,idx,c);if(m>bestM){bestM=m;bestC=c;bestT=t;bestIdx=idx;}}}}double c0=bestC;int t0=bestT;double fineStep=(p.name=="DEEP"?0.5:1.0);
